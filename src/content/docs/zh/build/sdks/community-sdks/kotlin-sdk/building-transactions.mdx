---
title: "构建并发送交易"
description: "学习如何使用 Kaptos Kotlin SDK 构建、签名并提交交易。"
sidebar:
  label: "构建交易"
---

import { Aside } from '@astrojs/starlight/components';

Kaptos Kotlin SDK 通过 `aptos.transactions` 提供基于命名空间的交易操作。

<Aside type="note">
所有这些方法都返回一个 `AptosResult<T>`。您可以使用 `when` 进行模式匹配，或使用扩展函数（如 `.map()`、`.fold()`、`.getOrNull()`、`.onSuccess()` 和 `.onFailure()`）来链式调用并处理错误。
</Aside>

## 创建账户（设置）

首先，我们来创建测试账户和客户端配置：

```kotlin
val aptos = Aptos(AptosConfig(network = Network.DEVNET))
val alice = Account.generate()
val bob = Account.generate()
aptos.faucet.fund(alice.accountAddress, 100_000_000UL)
```

## 构建负载 (Payload)

创建一个 entry function 负载，来描述您想调用的链上函数。`entryFunctionPayload` 会自动获取链上 ABI，将参数编码为 BCS 字节。

```kotlin
val payload = aptos.transactions.entryFunctionPayload(
  function = "0x1::coin::transfer",
  typeArguments = listOf(TypeTag.from("0x1::aptos_coin::AptosCoin")),
  arguments = listOf(
    MoveArgument.from(bob.accountAddress),
    MoveArgument.from(1_000_000UL),
  ),
)
```

## 构建交易

构建一个结合了发送方和负载的原始交易。

```kotlin
val txn = aptos.transactions.build(
  sender = alice.accountAddress,
  payload = payload,
)
```

您可以选择指定 `TransactionOptions`，例如您愿意支付的最高 gas 数量。

```kotlin
val txn = aptos.transactions.build(
  sender = alice.accountAddress,
  payload = payload,
  options = TransactionOptions(maxGasAmount = 20_000UL),
)
```

## 签名交易

签署该交易以创建发送方验证器。

```kotlin
val auth = aptos.transactions.sign(signer = alice, transaction = txn)
```

## 提交交易

将签署好的交易提交到网络。

```kotlin
val pending = aptos.transactions.submit(
  transaction = txn,
  senderAuthenticator = auth,
)
```

## 等待确认

等待提交的交易处理完成。

```kotlin
val confirmed = aptos.transactions.waitForTransaction(pending)
```

## 签名、提交并等待（一体化）

您可以将构建、签名、提交和等待组合成一次调用：

```kotlin
val confirmed = aptos.transactions.submitAndWait(
  signer = alice,
  payload = payload,
)
```

## 签名并提交

如果您只想进行签名和提交，而不想等待确认：

```kotlin
val pending = aptos.transactions.signAndSubmit(
  signer = alice,
  payload = payload,
)
```

## 多代理交易 (Multi-Agent Transactions)

构建需要多个签名者的交易：

```kotlin
val txn = aptos.transactions.buildMultiAgent(
  sender = alice.accountAddress,
  secondarySigners = listOf(bob.accountAddress),
  payload = payload,
)
```

## 模拟交易

您可以模拟一笔交易以查看其效果，而无需在网络上真正执行它。

```kotlin
val simResult = aptos.transactions.simulate(
  transaction = txn,
  senderPublicKey = alice.publicKey,
)
```
