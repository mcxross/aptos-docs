diff --git a/dist/index.d.ts b/dist/index.d.ts
index 3dbade370d1565aa795d48d63c5e237a6ada041d..a4e65afb10dbfebbfd6f5baad018bdc93d6dfc9b 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -52,7 +52,14 @@ export interface VercelServerlessConfig {
      * Here the list of the headers that are added:
      * - The CSP header of the static pages is added when CSP support is enabled.
      */
-    staticHeaders?: boolean;
+    staticHeaders?: boolean | {
+        /**
+         * How to emit CSP for prerendered/static pages when Astro CSP is enabled.
+         * - 'per-route' (default): one `routes` entry per path with a CSP header
+         * - 'global': collapse to a single catch-all `/(.*)`  route with the CSP header
+         */
+        cspMode?: 'per-route' | 'global';
+    };
 }
 interface VercelISRConfig {
     /**
diff --git a/dist/index.js b/dist/index.js
index f99edd8647b5b2519c431ab87b3843b1aca855f8..3792faa60941c47241dc6154bc050774f17598db 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -396,8 +396,19 @@ function vercelAdapter({
           if (!normalized.routes) {
             normalized.routes = [];
           }
-          if (staticHeaders) {
-            const routesWithConfigHeaders = createRoutesWithStaticHeaders(_routeToHeaders, _config);
+          const _staticHeadersEnabled =
+            staticHeaders === true || typeof staticHeaders === 'object';
+          const _cspMode =
+            typeof staticHeaders === 'object' && staticHeaders?.cspMode
+              ? staticHeaders.cspMode
+              : 'per-route';
+
+          if (_staticHeadersEnabled) {
+            const routesWithConfigHeaders = createRoutesWithStaticHeaders(
+              _routeToHeaders,
+              _config,
+              _cspMode
+            );
             const fileSystemRouteIndex = normalized.routes.findIndex(
               (r) => "handle" in r && r.handle === "filesystem"
             );
@@ -538,20 +549,29 @@ function getRuntime(process2, logger) {
   }
   return "nodejs24.x";
 }
-function createRoutesWithStaticHeaders(staticHeaders, config) {
+function createRoutesWithStaticHeaders(staticHeaders, config, cspMode = 'per-route') {
   const vercelHeaders = [];
+  if (!config?.security?.csp) return vercelHeaders;
+
+  if (cspMode === 'global') {
+    let csp;
+    for (const { headers } of staticHeaders.values()) {
+      const v = headers.get('Content-Security-Policy');
+      if (v) { csp = v; break; }
+    }
+    if (csp) {
+      vercelHeaders.push({ src: '/(.*)', headers: { 'content-security-policy': csp } });
+    }
+    return vercelHeaders;
+  }
+
   for (const [pathname, { headers }] of staticHeaders.entries()) {
-    if (config.security.csp) {
-      const csp = headers.get("Content-Security-Policy");
-      if (csp) {
-        const _headers = {
-          "content-security-policy": csp
-        };
-        vercelHeaders.push({
-          src: pathname,
-          headers: _headers
-        });
-      }
+    const csp = headers.get("Content-Security-Policy");
+    if (csp) {
+      vercelHeaders.push({
+        src: pathname,
+        headers: { "content-security-policy": csp }
+      });
     }
   }
   return vercelHeaders;
